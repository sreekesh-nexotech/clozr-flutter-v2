<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app is API-backed, so it needs the network in RELEASE too. Flutter's
         template declares INTERNET only in src/debug/ and src/profile/ (for hot
         reload), which is why a release build silently ships with no network
         permission and every request fails on a real install. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Attachments are chosen through file_picker, which uses the system
         document picker (Storage Access Framework). That hands the app a
         content URI for the chosen file only, so no storage or media
         permission is needed. Do not add READ_MEDIA_IMAGES /
         READ_EXTERNAL_STORAGE here: Google Play rejects them unless broad
         photo access is the app's core purpose. -->

    <application
        android:label="Clozr"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- The Call action hands a `tel:` number to the dialler. Android 11+
             hides other apps unless they are declared here, so without this
             `canLaunchUrl` reports false on a device that can dial perfectly
             well. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- Opening an attachment hands its https link to a browser or a
             document viewer. Same package-visibility rule as the dialler: on
             Android 11+ the launch fails unless the intent is declared. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
